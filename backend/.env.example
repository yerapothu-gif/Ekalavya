# Supabase project settings — Person 1 shares real values via DM, never commit them
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Owned by Person 6, key lives here so the AI module can read it server-side
OPENAI_API_KEY=

PORT=4000
